# Vercel Environment Variables
# Copy these to your Vercel project settings

# CRITICAL: Add these in Vercel Dashboard → Settings → Environment Variables

# Database
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/production-db?retryWrites=true&w=majority

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-min-32-characters-long
JWT_EXPIRE=7d
JWT_COOKIE_EXPIRE=7

# Node Environment (Vercel sets this automatically)
NODE_ENV=production

# CORS - Your Vercel domain will be added automatically
# Add any additional allowed origins here (comma-separated)
ALLOWED_ORIGINS=https://your-app.vercel.app,https://your-custom-domain.com

# File Upload (Vercel has /tmp directory)
MAX_FILE_SIZE=10485760
UPLOAD_PATH=/tmp/uploads

# WhatsApp/Twilio (Optional)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_NUMBER=

# Logging
LOG_LEVEL=error

# IMPORTANT: Vercel automatically sets these:
# - VERCEL=1
# - VERCEL_URL (deployment URL)
# - VERCEL_ENV (production/preview/development)
